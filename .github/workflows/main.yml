name: react s3 build
on:
  push:
    branchs:
      - main

jobs:
  build:
    name: build for deploy
    runs-on: ubuntu-18.04
    steps:
      - name: checkout source code
        uses: actions/checkout@main

      - name: install dependencies
        run: npm install

      - name: build
        run: npm build

      - name: deploy
        env:
          AWS_ACCESS_KEY_ID: ${{ AKIA4QYA2VK6QIKCPBNK }}
          AWS_SECRET_ACCESS_KEY: ${{ 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 }}
        run: |
          aws se cp \
            --recursive \
            --region ap-northeast-2 \
            build s3: lis-front-deploy-bucket
